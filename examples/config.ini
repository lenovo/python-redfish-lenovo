[ConnectCfg]
# BMC IP address(example:10.10.10.10)
BmcIp = 
# BMC user name(example:USERID)
BmcUsername = 
# BMC user password(example:PASSW0RD)
# If you have special char '%' in password, '%' must be followed by '%' 
BmcUserpassword = 
# ComputerSystem instance id(None: first instance, All: all instances)
SystemId =
# Authentication mode(session or basic), the default is session
Auth = session
# Security certificate file for SSL connections, the default is null string
Cafile = 

[FileServerCfg]
### FileServerCfg for "firmware update"/"mount virtual media"/"export ffdc" ###
# File server protocol(example:SFTP)
FSprotocol =
# File server port(example:22)
FSport =
# File server ip(example:10.10.10.10)
FSip =
# File server user name(example:mysftp)
FSusername =
# File server user password(example:PASSW0RD)
FSpassword =
# File server path
FSdir =

### FileServerCfg for SR635/SR655 server's "Bmc config backup"/"Bmc config restore" ###
# http file server ip for SR635/SR655
Httpip =
# http file server port for SR635/SR655, default port is 80
Httpport =
# the directory on http file server for SR635/SR655
Httpdir =
